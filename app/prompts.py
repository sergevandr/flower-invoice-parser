INVOICE_PARSE_PROMPT = """
        Ты анализируешь фото закупочной накладной с цветами.
        
        Верни только валидный JSON.
        
        Формат:
        {
          "supplier": "название поставщика",
          "items": [
            {
              "raw_name": "название товара максимально близко к тексту на фото",
              "qty": число,
              "price": число
            }
          ],
          "invoice_number": "номер накладной или null",
          "invoice_date": "дата накладной в формате YYYY-MM-DD или null"
        }
        
        Правила:
        - извлекай все товарные строки
        - не заменяй один вид цветка на другой (например гербера ≠ гвоздика ≠ тюльпан)
        - если не уверен в виде цветка, оставь название как есть, не исправляй
        - не переводи названия
        - если слово читается неуверенно, сохрани наиболее близкую к фото версию
        - не додумывай ботанический вид товара
        - не включай итоги, суммы, доставку, скидки, подписи
        - если фото не читается, верни {"supplier": null, "items": []}
        - если номер накладной не найден → верни null
        - если дата накладной не найдена → верни null
        - дату старайся привести к формату YYYY-MM-DD
        - ответ только JSON
    """
MANDRYKIN_PROMPT = """
Ты извлекаешь данные из накладной поставщика ИП Мандрыкин.

Извлеки данные строго по структуре документа.

Используй только следующие колонки:

- Название товара бери из колонки: "Наименование товара (описание выполненных работ, оказанных услуг, имущественного права)"
- Количество бери из колонки: "Количество (объем)"
- Общую сумму по строке бери из колонки: "Стоимость товара (работ, услуг), имущественных прав без налога - всего"

Правила расчета:
- price (цена за единицу) НЕ бери из документа напрямую
- вычисли price как: total_sum / qty
- округли price до 2 знаков после запятой

Важно:
- количество должно быть реальным количеством товара (обычно небольшое число, например 1–100)
- используй только указанные колонки
- не пытайся интерпретировать другие числа в строке

Извлеки также:
- supplier
- invoice_number
- invoice_date

Верни результат строго в формате JSON:

{
  "supplier": "...",
  "invoice_number": "...",
  "invoice_date": "...",
  "items": [
    {
      "raw_name": "...",
      "qty": число,
      "price": число
    }
  ]
}

Не добавляй никакого текста вне JSON.
"""

SUPPLIER_PARSE_PROMPT = """
Ты анализируешь фото закупочной накладной.

Нужно определить только поставщика / продавца / контрагента.

Верни строго JSON без markdown:

{
  "supplier": "название поставщика"
}

Правила:
- ищи именно поставщика / продавца
- не возвращай покупателя, грузополучателя, адрес, банк, ИНН, телефон, менеджера
- если есть ИП или ООО, сохрани это
- если не уверен, верни наиболее вероятное название поставщика
- ответ только JSON
"""